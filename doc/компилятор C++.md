# компилятор C++

- для разработка под Linux рекомендуется штатный GNU GCC/G++
- как вариант: LLVM/Clang

```sh
# Установка GCC на Ubuntu/Debian
sudo apt install -uy g++ gdb build-essential

# Проверка установки
g++ --version
```
