# 1.2.2. Применение параллелизма для повышения производительности

разбить задачу на части и запустить их параллельно, уменьшив тем самым адачу на части и запустить их параллельно, уменьшив тем самым общее время выполнения. Это бщее время выполнения. Это **распараллеливание по задачам**

каждый поток выполняет одну и ту же операцию, но с разными данными. Последний вариант называется е операцию, но с разными данными. Последний вариант называется **распараллеливание по данным** .

Алгоритмы, легко поддающиеся такому распараллеливанию, час- лгоритмы, легко поддающиеся такому распараллеливанию, часто называют о называют естественно параллельными стественно параллельными (embarrassingly parallel, (embarrassingly parallel, naturally parallel, conveniently concurrent.). Они очень хорошо мас- aturally parallel, conveniently concurrent.). Они очень хорошо масштабируются – если число располагаемых аппаратных потоков уве- табируются – если число располагаемых аппаратных потоков увеличивается, то и степень параллелизма алгоритма возрастает. 

